#3. Create a Python expression to calculate the area of a circle.
radius = float(input("Enter the number:"))
area = 3.14 * radius +radius #(fomula of area = pi * r * r)
print("area of a circle", area)